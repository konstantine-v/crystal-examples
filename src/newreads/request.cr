
module NewReads::Request

  def self.check
    # Check to see if ISBN is valid or doesn't contain illegal characters
  end

  def self
    # puts "Your GCP Auth Token: " + ENV["GCP_TOKEN"]

    # puts "Searching for book..."
    # r = Halite.follow.get("https://www.googleapis.com/books/v1/volumes", headers: {
    #   "Private-Token" => ENV["GCP_TOKEN"]
    # }, params: {
    #   "q" => "identifier%#{ISBN}"
    # })

    # puts "Here's the results: "
    # puts r.body
    # puts r.body[1]

    # puts checkBook["totalItems"]
    # puts checkBook["kind"]
    # response = JSON.parse(checkBook)
    # while checkBook[totalItems] === 0
    #   ISBN = LANG.sample() + RAND_ISBN.rand(999999999).to_s

  end
  

end